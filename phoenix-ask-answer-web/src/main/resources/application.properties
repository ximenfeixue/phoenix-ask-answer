server.port:6666
server.compression.enabled: true
server.compression.min-response-size: 1

# SECURITY OAUTH2 RESOURCES (ResourceServerProperties)
#security.oauth2.resource.id=directory
#security.oauth2.authenticated=/directory/directory/**,/directory/type/**

#error
#server.error.path=/directory/error

# HTTP encoding (HttpEncodingProperties)
spring.http.encoding.charset=UTF-8
spring.http.encoding.enabled=true
spring.http.encoding.force=true


#test myself write logger filter
parasol.logger.enabled=true
parasol.logger.parameter=parameter_11111
parasol.logger.urlPatterns=/*

logging.level.org.springframework.web=DEBUG
logging.level.com.ginkgocap.parasol.directory.web.jetty.web.filter=INFO
logging.pattern.level=user:%X{user} %5p


#dubbo
dubbo.application.name=${dubbo.application.name}
dubbo.registry.address=${dubbo.registry.address}
dubbo.protocol.name=${dubbo.protocol.name}

#redis
redis.host=${redis.host}
redis.port=${redis.port}

#error
